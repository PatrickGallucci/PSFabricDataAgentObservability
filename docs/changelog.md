# Changelog

The authoritative changelog lives at the repository root in
[`CHANGELOG.md`](https://github.com/PatrickGallucci/PSFabricDataAgentObservability/blob/main/CHANGELOG.md).

> The documentation build copies the root `CHANGELOG.md` over this page so the
> published site always shows the latest release notes.
